dist
node_modules
coverage
package-lock.json

# Plugin ESLint local : déjà exclu d'ESLint (eslint.config.mjs), exclu aussi de Prettier
# pour éviter que le reformatage casse les directives `@ts-ignore` sensibles au retour à la ligne.
eslint-rules

# Documentation : mise en forme manuelle (tableaux alignés) préservée.
*.md

# Migrations Drizzle : SQL et snapshots meta/ générés par drizzle-kit, ne pas reformater
# (drizzle-kit réécrit ces fichiers dans son propre format ; le reformatage créerait du bruit).
src/infrastructure/persistence/drizzle/migrations
